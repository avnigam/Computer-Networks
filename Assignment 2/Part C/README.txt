To run the code you need to have dpkt installed in your python.
This code runs only on python 2.7 due to dpkt dependency.

Place http_8092.pcap, tcp_8093.pcap and tcp_8094.pcap file in the same folder as the "analysis_pcap_http.py" file.

To run the code:
python analysis_pcap_http.py